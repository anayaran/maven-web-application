version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn clean install
artifacts:
  files:
    - /target/maven-web-application.war
    - appspec.yml
  discard-paths: yes

